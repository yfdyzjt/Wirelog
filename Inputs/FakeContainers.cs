using Terraria.Audio;
using Terraria;

namespace Wirelog.Inputs
{
    public static class FakeContainers
    {
        public static void Activate(Input input)
        {
            // int num = Main.tile[input.Pos].TileFrameX / 18 * -1;
            // int num2 = Main.tile[input.Pos].TileFrameX / 18 * -1;
            // num %= 4;
            // if (num < -1)
            // {
            //     num += 2;
            // }
            // num += input.Pos.X;
            // num2 += input.Pos.Y;
            // SoundEngine.PlaySound(28, i * 16, j * 16, 0, 1f, 0f);
        }
    }
}